# Q1: How many employees does the city have?
import helper #import module

data = helper.read_salaries() #read the data

employee_count = len(data) #count the length of the data

print(employee_count) #print out the data length 
